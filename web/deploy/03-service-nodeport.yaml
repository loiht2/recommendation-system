apiVersion: v1
kind: Service
metadata:
  name: web-ui
  namespace: profiler
  labels:
    app: web-ui
spec:
  type: NodePort
  selector:
    app: web-ui
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30090
      protocol: TCP
